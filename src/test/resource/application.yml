server:
  port: 8080
  
management:
  security:  
    enabled: false 
    
spring:
  application:
    name: skipthedishes-sp-fair
#    profiles:
#    active: default   
  jpa:
      database: MYSQL
      hibernate:
        ddl-auto: none
#        dialect:  org.hibernate.dialect.MySQLDialect        
  datasource:
        url: jdbc:mysql://127.0.0.1:3306/mysql?zeroDateTimeBehavior=convertToNull&amp;useUnicode=yes&amp;characterEncoding=UTF-8
        username: root
        password: skipthedishes-sp-fair
        driver-class-name: com.mysql.jdbc.Driver              
